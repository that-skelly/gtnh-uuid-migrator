# gtnh-uuid-migrator
A small Python tool for migrating Better Questing progress from one UUID to another. Supports both single-file QuestProgress.json setups and GTNH-style QuestProgress/&lt;uuid>.json layouts, with optional Better Questing party membership syncing for account migration, UUID changes, and recovery.
